server:
  port: 80

spring:
  application:
    name: cloud-consumer-consul-order
  cloud:
    # 注册到consul
    consul:
      host: 172.16.201.128
      port: 8500
      discovery:
        service-name: ${spring.application.name}